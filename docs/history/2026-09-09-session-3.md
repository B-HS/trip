# 2026-09-09 — 세션 3 (로드맵 7 Tiptap 에디터, Workflow 운용 전환)

## 한 일

- 컨텍스트 복원: HANDOFF·ARCHITECTURE·PROCESS·roadmap·ADR 0017~0030·data-model·리서치·history·bug·QA 정독, 지정 코드 9파일 + 마이그레이션·DB(테이블 26, 이력 6행)·`'use no memo'` 16파일 대조. 불일치 4건 보고(문서 헤더 stale, `llm-rules.auto-push` false, dev 서버 PID 교체, Vercel CLI 구버전). 질문 4건 전부 추천안 승인 → ADR-0031(`94c6c55`).
- 운용 전환: `Agent` 도구 금지, 위임은 `Workflow` 의 `agent()` 로만. 구현 Opus max·리뷰 Opus high·사실 확인 Sonnet. `git config llm-rules.auto-push true`.
- 로드맵 7(ADR-0027): 의존성 고정(Tiptap 3.31.3 8종, isomorphic-dompurify 4.2.0, happy-dom → dependencies) 뒤 Workflow `roadmap-7-tiptap-editor`(Facts Sonnet → Server Opus max → Editor Opus max → 리뷰 4렌즈 Opus high → Fix Opus max, 8 에이전트 51분). 산출: `shared/constant/rich-text.ts`, `shared/lib/rich-text-{extensions,document,sanitize,html}.ts`, `features/editor/*` 7파일, `app/globals.css` `.rich-text`, 테스트 5파일 49케이스. 리뷰 17건 중 14 적용(화이트리스트 sanitize·훅 범위 한정·대문자 스킴·`isAllowedUri`·브랜드 타입·server-only+테스트 mock·다이얼로그 공통화 등), 오탐 1(editorProps useRef — React Compiler 가 이미 메모이즈), 범위 밖 2 는 메인이 처리(`UPLOAD_DISABLED_HINT` shared 승격, ARCHITECTURE §2·§12).

## 검증

- typecheck·lint·prettier·`bun test` 305/305·`bun run build` 통과(메인 재실행). 브라우저 실측·서버 번들 검증은 로드맵 6 에서(ADR-0031).

## 남은 것

- 4-4 로드맵 6(ADR-0028) → 5단계 로드맵 4(ADR-0029) → 6단계 로드맵 10(ADR-0030).
- 사용자 작업: `.env`·Vercel 에 `APP_ENCRYPTION_KEY`·`R2_*`, `.env.example` 이름, R2 커스텀 도메인, Vercel CLI 최신화 + `vercel link`.
